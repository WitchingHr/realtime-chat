"use client"

import Button from "../components/ui/Button";

interface pageProps {}

const page: React.FC<pageProps> = ({}) => {
  return (
    <div>
      <Button size={'sm'}>Hello</Button>
    </div>
  );
};

export default page;
